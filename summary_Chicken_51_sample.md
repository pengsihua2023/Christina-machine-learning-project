# 家禽合并队列（n=51）SVM-RBF 建模结果总结

**队列**：PRJNA644054（火鸡，45）+ PRJNA379944（鸡，6）= 51 个样本
**方法**：SVM-RBF，原始计数 + `PrevalenceCLR` 折内拟合，嵌套交叉验证
**脚本**：`poultry_cohort.py` ｜ **结果**：`results/poultry_cohort.json`、`results/poultry_*.csv`
**日期**：2026-08-11

> **一句话结论：合并后 AUC 0.895，但比火鸡单独建模（0.930）更差——建议不要合并。**

---

## 1. 队列构成

| BioProject | 宿主 | 研究中心 | Neg | Pos | n |
|---|---|---|---|---|---|
| PRJNA644054 | 火鸡 | Ohio State | 13 | 32 | 45 |
| PRJNA379944 | 鸡 | 华南农业大学 | 3 | 3 | 6 |
| **合计** | | | **16** | **35** | **51** |

阳性率 68.6%，**基线 accuracy = 0.686**（主队列 PRJNA464410 为 0.581）。

**注**：本文件名沿用用户指定的 `Chicken`，但队列实际以火鸡为主（45/51 = 88%），鸡仅 6 个样本。

### 与主队列（鸭，n=260）的三点结构性差异

| | 主队列 | 本队列 |
|---|---|---|
| n | 260 | **51** |
| 少数类 | 109 | **16** |
| 基线 accuracy | 0.581 | 0.686 |
| **p/n**（流行度 ≥10%） | 70/260 = **0.27** | 68/51 = **1.33** |
| 置换零分布最大值 | 0.614 | **0.798** |

**p/n 从 0.27 翻转到 1.33——这已是 p > n 的高维问题**，模型可完美拟合训练集。

---

## 2. 主结果：SVM-RBF 嵌套 CV

外层 RepeatedStratifiedKFold（5 折 × 10 次 = 50 折），内层 4 折调参，流行度阈值 0.10（保留 68 个特征）。

| 指标 | 值 | 备注 |
|---|---|---|
| **ROC-AUC** | **0.895** | |
| PR-AUC | 0.952 | 基线 = 阳性率 0.686 |
| Accuracy | 0.825 | 基线 0.686，真实增益 **+0.139** |
| 平衡准确率 | 0.759 | |
| 灵敏度 | 0.937 | |
| **特异度** | **0.581** | ⚠️ 见 §5 |
| MCC | 0.576 | |

选中超参：`C = 1.0, gamma = 'scale'`

**混淆矩阵**（50 折折外预测合并，计数已除以重复次数 10）：

| | 预测 Neg | 预测 Pos |
|---|---|---|
| **真实 Neg** | 9.3 | 6.7 |
| **真实 Pos** | 2.2 | 32.8 |

**16 个阴性中判错 6.7 个。**

---

## 3. 置换检验

```
实测 AUC     0.967   （单层 CV、固定超参；嵌套 CV 的诚实估计为 0.895）
零分布       0.492 ± 0.121   最大值 0.798
经验 p 值    0.0050
```

**通过，但余量比主队列小得多。**

零分布标准差 0.121（主队列为 0.049），**最大值达 0.798**——意味着在 n=51 下，纯噪声就能跑出接近 0.8 的 AUC。**因此本队列中低于 0.80 的 AUC 基本不具解释力。**

---

## 4. 流行度阈值扫描

n=51 下 p/n 是首要问题，故先扫描过滤阈值（固定超参 C=5，单层 CV，仅供比较趋势）：

| 流行度 | 特征数 | p/n | AUC |
|---|---|---|---|
| ≥ 0.05 | 92 | 1.80 | 0.976 |
| **≥ 0.10** | **68** | **1.33** | **0.976** ← 采用 |
| ≥ 0.15 | 55 | 1.08 | 0.940 |
| ≥ 0.20 | 51 | 1.00 | 0.945 |
| ≥ 0.30 | 45 | 0.88 | 0.949 |
| ≥ 0.40 | 39 | 0.76 | 0.955 |

**AUC 对阈值不敏感**（0.940–0.976），说明结论不依赖这个选择。为与主队列保持一致，主结果仍用 0.10。

---

## 5. 三个必须一并报告的问题

### 5.1 特异度仅 0.581

模型基本在顺着 68.6% 的阳性率猜。灵敏度 0.937 好看，是因为阳性占多数。

**16 个阴性样本意味着 5 折 CV 下每折测试集只有约 3 个阴性——一个样本判错，特异度就跳 33 个百分点。** 这个数字的置信区间极宽。

### 5.2 LOO 与 5 折相差 0.078

```
留一法 LOO        AUC 0.973
5 折 × 10 重复     AUC 0.895
```

差距反映**划分方差极大**。LOO 训练集更大因而更乐观，但两者都不应单独引用。**嵌套 5 折的 0.895 是更保守、更可辩护的数字。**

### 5.3 两个 project 在技术上完全不可比

以下四个属性与 BioProject **完全共线**：

```
宿主            火鸡 45 / 鸡 6
研究中心        Ohio State / 华南农大
Assay.Type      OTHER / CTS
LibrarySource   OTHER / METAGENOMIC
```

合并等于把两批用不同建库方法、在不同实验室、对不同物种测的数据放在一起。

---

## 6. 批次检查：是稀释，不是混杂

```
菌群 → 预测 project（鸡 vs 火鸡）   AUC 1.000   ← 完全可分
project → 预测标签                 AUC 0.499   ← 毫无信息
```

这两个数字合起来的含义很明确：

- **两个 project 的菌群完全不同**（AUC 1.000）——不同宿主 + 不同实验室，意料之中
- **但 project 归属与感染状态无关**（0.499）

**因此批次效应不是混杂，而是稀释。** 它没有虚高 AUC，反而给模型增加了必须绕开的无关变异。

> 与主队列对照：那里采样月份既影响菌群**也**影响标签（仅协变量即达 AUC 0.881），是真正的混杂；本队列的批次只影响特征、不影响标签，性质不同。

---

## 7. 决定性对照：合并使性能下降

| | n | 阳性 | 基线 acc | AUC | Accuracy | 特异度 | MCC |
|---|---|---|---|---|---|---|---|
| **仅火鸡 PRJNA644054** | 45 | 32 | 0.711 | **0.930** | **0.860** | **0.677** | **0.647** |
| 合并 n=51 | 51 | 35 | 0.686 | 0.895 | 0.825 | 0.581 | 0.576 |
| | | | | **Δ = −0.035** | −0.035 | −0.096 | −0.071 |

**加入那 6 个鸡样本后，四项指标全部下降。**

原因见 §6：这 6 个样本来自完全不同的体系，其菌群与火鸡完全可分，但携带的感染信息与火鸡不通用——对模型而言是纯噪声。

---

## 8. 结论与建议

### 8.1 直接回答

**合并队列（n=51）的 SVM-RBF 结果为 AUC 0.895、Accuracy 0.825（基线 0.686）、MCC 0.576，置换检验 p=0.0050。**

### 8.2 建议：不要合并，用 PRJNA644054 单独建模

三条理由：

1. **合并后四项指标全部下降**（§7）
2. **6 个样本（3 阳 3 阴）无法支撑任何跨宿主结论**
3. **火鸡单独的 AUC 0.930、MCC 0.647，是本项目除主队列外最好的结果**

### 8.3 若目的是检验跨宿主泛化

正确做法是"训练火鸡、测试鸡"而非合并。但 **6 个样本（3/3）也做不了这件事**——AUC 会完全由个别样本决定，任何结果都不可解释。

### 8.4 若火鸡队列要写进论文，尚需补做

n=45、少数类 13，**比主队列小 5 倍**，需要与主队列同等强度的审查：

- [ ] PRJNA644054 内部是否存在类似"采样月份"的混杂结构
- [ ] permutation importance 与差异丰度分析
- [ ] 其 biomarker 与鸭队列九菌交集是否重叠（跨宿主一致性的间接证据）
- [ ] 该队列的 `results/` 存档与一致性检查纳入 `check_consistency.py`

---

## 9. 复现

```bash
python3 poultry_cohort.py                              # 默认流行度 0.10，置换 200 次
python3 poultry_cohort.py --prevalence 0.20 --n-perm 500
```

输出：

| 文件 | 内容 |
|---|---|
| `results/poultry_cohort.json` | 全部指标、批次检查、对照结果 |
| `results/poultry_prevalence_scan.csv` | 流行度阈值扫描 |
| `results/poultry_permutation_null.txt` | 200 次置换的零分布 |
| `results/poultry.log` | 完整运行日志 |

---

## 10. 报告本结果时的硬性要求

1. **AUC 必须与置换零分布最大值 0.798 并列** —— n=51 下噪声可达 0.8，脱离这个参照的 AUC 会被高估
2. **Accuracy 必须与基线 0.686 并列** —— 真实增益是 +0.139，不是 +0.325
3. **必须报告特异度 0.581** —— 只报灵敏度 0.937 会严重误导
4. **必须说明合并使性能下降** —— 否则读者会以为合并是改进
5. **不得引用 LOO 的 0.973** 作为主结果 —— 嵌套 5 折的 0.895 才是可辩护的估计
