# 生态特征空间 vs 属丰度模型

**脚本**：`eco_common.py`、`ecological_models.py`、`ecological_cage_check.py`、`ecological_transfer_anatomy.py`
**结果**：`results/ecological_models.json`、`ecological_models_depthadj.json`、`ecological_cage_check.json`、`ecological_depth_diagnostic.json`、`ecological_transfer_anatomy.json`、`ecological_transfer_linear.json`
**数据**：`ecological_model/`（见 `readme-ecological-results.md`）
**英文版**：`summary_ecological_models.md`（两版同步维护）
**日期**：2026-08-25

> **一句话结论：感染个体保留的宿主特异基线核心菌群更少，这一点在两个宿主里都成立。仅用核心保留度、在 260 只野鸭上训练的线性模型，把火鸡的感染与对照分开到 AUC 0.870（去深度混杂后 0.904，p=0.0020），而属丰度模型在目标队列上判别函数完全常数化。同一批生态特征还分不开笼号（3 个纯笼对比 0/3 显著），而属丰度分笼与分感染一样好（0.908 对 0.967）。**

---

## 0. 比较了哪些特征空间

| 特征空间 | 特征数 | 含分类学细节？ | 需折内拟合？ |
|---|---|---|---|
| α 多样性 | 6 | 否 | 无需拟合（逐样本） |
| 核心保留度 | 7 | 否 | **是** —— 用训练折阴性样本重定义核心 |
| **纯生态（α + 核心）** | **13** | **否** | **是** |
| 纯生态 + 结构（α + 核心 + Bray PCoA） | 23 | 否 | 是 |
| 群落结构 PCoA（Bray） | 10 | 否 | 是 —— 训练折做 MDS，测试样本 Gower 插值投影 |
| 群落结构核（Bray） | — | 否 | 无需拟合（距离是两两函数） |
| 群落结构 PCoA（Aitchison） | 10 | **是** —— 见下 | 是 |
| 群落结构核（Aitchison） | — | **是** | 无需拟合 |
| α + 核心 + Aitchison PCoA | 23 | **是** | 是 |
| **属丰度 CLR**（参照） | 鸭 70 / 火鸡 62 | 是 | 是 —— `PrevalenceCLR` |

**Aitchison 距离就是 CLR 空间的欧氏距离** —— 与分类学模型用的是同一份属丰度数据，只是换成了距离表示。任何建立在它之上的空间都携带完整的分类学信号，**不是**分类学的生态学替代品。表中保留它作为对照，并明确标注。

模型全程固定为 SVM-RBF，使变化的只有特征空间而非学习器。协议：外层 5 折 × 10 次，内层 4 折调参，AUC 逐折计算再平均。

**校准**：属丰度参照复现了既有结果 —— 鸭 70 特征、AUC 0.836（文档 0.835，README §4），火鸡 62 特征、0.972。§3 的笼效应对比复现 `summary_Turkey_45_sample.md` §4 到三位小数。Python 版核心保留度与协作者的 R 输出在全部 7 列、326 个样本上完全一致。

---

## 1. 队列内性能

| 特征空间 | 鸭（n=260） | p | 火鸡（n=45） | p |
|---|---|---|---|---|
| α 多样性 | 0.538 | 0.333 | 0.748 | 0.070 |
| 核心保留度 | 0.593 | **0.050** | 0.799 | **0.010** |
| **纯生态（α + 核心）** | **0.613** | **0.010** | **0.807** | **0.020** |
| 纯生态 + Bray | 0.674 | **0.005** | 0.854 | **0.005** |
| PCoA（Bray） | 0.661 | **0.005** | 0.813 | **0.005** |
| 核（Bray） | 0.695 | **0.005** | 0.850 | **0.005** |
| *PCoA（Aitchison）—— 分类学* | *0.808* | *0.005* | *0.962* | *0.005* |
| *核（Aitchison）—— 分类学* | *0.848* | *0.005* | *0.970* | *0.005* |
| *α + 核心 + Aitchison —— 分类学* | *0.773* | *0.005* | *0.916* | *0.005* |
| **属丰度** | **0.836** | **0.005** | **0.972** | **0.005** |

**单论区分能力，属丰度模型胜出，在鸭队列上差距不小**（0.836 对最佳纯生态空间的 0.674）。火鸡队列差距收窄（0.972 对 0.854）但仍存在。

生态这一块内部有两点值得注意：

- **α 多样性单独不可用**，两个队列都过不了自己的置换零分布（鸭 p=0.333，火鸡 p=0.070）。
- **α 与核心保留度合并后优于任一单项**（鸭 0.613 对 0.538 与 0.593），且两个队列都达到显著。注意这是**队列内**的说法；跨宿主迁移时，§5 显示核心保留度单独反而更好。

---

## 2. 测序深度是真实的混杂因素

计数未做抽平，而丰富度指标天然随深度上升。

| 队列 | 深度 → 预测感染状态 AUC |
|---|---|
| 鸭 | 0.650 |
| 火鸡 | 0.560 |
| **天鹅（n=15）** | **0.870** |

天鹅队列里，深度单独就几乎能分开两类，且每个丰富度型特征都紧随深度：

| 特征 | 单独 AUC | 与深度 \|r\| |
|---|---|---|
| `TotalTaxaPresent` | 1.000 | 0.772 |
| `Observed_Genera` | 1.000 | 0.618 |
| `Shannon_Genus` | 1.000 | 0.713 |
| `CoreRetentionProportion` | 0.980 | 0.712 |
| `CoreAbundanceRetention` | 0.560 | **0.007** |

最后一行是内部对照：唯一与深度基本无关的核心指标，也正是唯一没有信号的那个。

**去深度混杂的敏感性分析**（`--deconfound-depth`；每个特征对 log 深度回归，系数折内拟合）同时说明了两件事，都增强了后文的可信度：

| | 鸭 主 → 去混杂 | 火鸡 主 → 去混杂 |
|---|---|---|
| α 多样性 | 0.538 → 0.540 | 0.748 → 0.752 |
| 核心保留度 | 0.593 → 0.578 | 0.799 → **0.828** |
| 纯生态（α + 核心） | 0.613 → 0.603 | 0.807 → **0.817** |
| 纯生态 + Bray | 0.674 → 0.664 | 0.854 → 0.854 |
| 属丰度 | 0.836 → 0.836 | 0.972 → 0.972 |

1. **队列内结果几乎不动**，说明 §1 对深度稳健。
2. **天鹅的迁移结果全部崩塌** —— 纯生态模型的「火鸡 → 天鹅」从 AUC 1.000 掉到 0.600（p 0.005 → 0.259）。深度假象就该这样表现，这反过来印证了上面的诊断。**天鹅队列的任何结果都不可解读**，已从结论中剔除。

---

## 3. 生态特征受笼效应污染小得多

> **这里的「笼」指饲养单元，即每个养 8 只鸟的隔离器** —— 负压饲养单元，组内共享垫料、饮水与饲料。术语沿用文献惯例，物理单元并非单只动物的笼子。元数据中记录空间分组的列只有 `Isolator_.`。

`summary_Turkey_45_sample.md` §4 已确立：在属丰度空间里，纯笼效应（毒株、批次、感染状态全部固定，只变隔离器编号）达到 AUC 0.908，对照感染效应 0.967 —— 两者无法区分。同样三组对比在每个特征空间里各跑一遍：

| 特征空间 | 纯笼（3 组均值） | 感染 | 笼 / 感染 | 笼对比显著数 |
|---|---|---|---|---|
| α 多样性 | 0.502 | 0.746 | **0.67** | 0/3 |
| 纯生态 + Bray | 0.580 | 0.856 | **0.68** | 0/3 |
| **纯生态（α + 核心）** | **0.563** | **0.784** | **0.72** | **0/3** |
| PCoA（Bray） | 0.594 | 0.827 | **0.72** | 0/3 |
| 核心保留度 | 0.602 | 0.780 | **0.77** | 0/3 |
| *α + 核心 + Aitchison —— 分类学* | *0.773* | *0.919* | *0.84* | *1/3* |
| *PCoA（Aitchison）—— 分类学* | *0.935* | *0.976* | *0.96* | *3/3* |
| **属丰度** | **0.908** | **0.967** | **0.94** | **3/3** |

纯生态模型的细节：

```
隔离器 3 vs 4（同 CKPA、同 10-30、同阳性）   AUC 0.556   p = 0.3987
隔离器 5 vs 6（同 TKMN、同 11-06、同阳性）   AUC 0.644   p = 0.2458
隔离器 1 vs 2（同 Mock、同 10-30、同阴性）   AUC 0.487   p = 0.5515
                                   均值    AUC 0.563   0/3 显著

感染 vs 对照（n=45）                      AUC 0.784   p = 0.0100
```

**每一个纯生态空间都测得出感染、测不出笼号；每一个分类学空间两者都测得出。** 这个次序随特征空间保留的分类学细节多少单调变化，正因如此它才可信，而不是巧合。

> **这条结论能与不能支持什么。** 它**不**表明生态特征测量感染更准 —— 0.784 远低于属丰度模型的 0.967。它表明的是：这个更低的分数**受污染更少**。在笼与感染完全共线的设计里，一个忽略笼级群落特异性、同时保留感染信号的特征，比一个无法归因于任何东西的更高数字更有价值。

---

## 4. 跨宿主迁移：最初的问题，在一个方向上得到回答

整个源队列训练，整个目标队列测试。距离核与 PCoA 无法跨队列（坐标系是队列内定义的），因此可比的是 α 多样性、核心保留度、二者合并与属丰度。每个 AUC 都配有目标标签的置换零分布。

| 源 → 目标 | α 多样性 | 核心保留度 | **纯生态（α + 核心）** | 属丰度 |
|---|---|---|---|---|
| **鸭 → 火鸡** | 0.375 (p=0.91) | 0.469 (p=0.62) | **0.800 (p=0.0050)** | 0.500 —— **退化** |
| 火鸡 → 鸭 | 0.503 (p=0.46) | 0.579 (p=0.035) | 0.549 (p=0.0945) | 0.500 —— **退化** |
| ~~鸭 → 天鹅~~ | ~~0.400~~ | ~~0.900~~ | ~~0.500~~ | ~~0.500~~ |
| ~~火鸡 → 天鹅~~ | ~~1.000~~ | ~~0.980~~ | ~~1.000~~ | ~~0.220~~ |

天鹅两行已划掉 —— 深度假象（§2），且去深度混杂后全部崩塌。

### 鸭 → 火鸡这一格

```
                      主分析        去深度混杂后
纯生态（α + 核心）      0.800         0.803
噪声上限（零最大）       0.740         0.757
p                     0.0050        0.0050
```

**一个在 260 只野鸭上训练、只用 α 多样性与核心保留度的模型，把火鸡的感染与对照分开到 AUC 0.800，高于自身噪声上限，且去深度混杂后不变。** 这是本项目里第一个以非退化方式越过自身噪声上限的跨宿主迁移。

三点值得注意：

1. **这个 0.800 低估了效应，因为 RBF 核在这里是错的模型。** 在 SVM-RBF 下 α 迁移 0.375、核心保留度 0.469，看上去只有 13 特征合并才管用。§5 表明那是核函数造成的假象：换成线性模型，**核心保留度单独就能迁移到 0.870**。上表保留 RBF 结果是为了与 §1–§3 协议一致，但正确的读法在 §5。
2. **属丰度模型不是迁移得更差，而是彻底崩塌。** 它在目标队列上的判别函数是常数。流行度过滤在鸭队列拟合，火鸡的 CLR 值远超训练范围，RBF 核下溢，所有测试样本得到相同分数。这里的 AUC 0.500 意思是「毫无区分力」，不是「随机水平的区分力」。
3. **目标宿主自身的属丰度信号本就受笼效应污染**，所以分类学迁移即便成功也难以解读。生态迁移没有这个问题（§3）。

### 这条结论**不**能确立什么

- **迁移是不对称的。** 火鸡 → 鸭为 0.549（p=0.0945），去深度混杂后 0.523（p=0.269）—— 无信号。用 45 个样本训练去预测 260 个本就是更难的方向，且火鸡自身信号与笼纠缠，所以不对称不意外；但这意味着能说的是「鸭训练的生态模型能迁移到火鸡」，而非「生态特征具有宿主普适性」。
- **一个方向、一对队列。** 天鹅队列不可用，没有第三个宿主可复现。
- **火鸡的感染状态与笼完全共线。** 鸭训练的模型从未见过笼，它学到的是鸭的感染信号 —— 但这个 0.800 是对着一个与笼分不开的火鸡标签测出来的。§3 缓解了这一点（生态特征不追踪笼号），但没有消除。

**正面回答最初的问题**：在这份数据上，更宏观的生态预测子**确实**比属丰度更能跨宿主保持一致 —— 而且是决定性的，因为分类学模型根本迁移不过去。证据仍限于一对队列的一个方向，但 §5 表明它落在一个可解释的单一量上，而非一个说不清的特征组合。

---

## 5. α + 核心里到底是什么在迁移

**脚本**：`ecological_transfer_anatomy.py` ｜ **结果**：`results/ecological_transfer_anatomy.json`、`ecological_transfer_linear.json`、`ecological_transfer_singles_corrected.json`

### 5.1 障碍是 RBF 核，不是特征

把每个迁移用 L2 逻辑回归重跑一遍：

| 特征集 | SVM-RBF | 线性 | 线性 + 去深度 |
|---|---|---|---|
| 全部 13 | 0.800 | 0.834 | 0.856 |
| 仅 α 多样性 | 0.375 | 0.714 | 0.716 |
| **仅核心保留度** | 0.469 | **0.870** | **0.904** |
| 无量纲子集（5） | 0.728 | 0.784 | — |
| 计数型子集（6） | 0.591 | 0.856 | — |

**线性模型在每个子集上都胜过 RBF，而且核心保留度单独就胜过 13 特征的 RBF 模型。** RBF 核度量的是标准化特征空间里的欧氏距离；标准化在鸭队列上拟合，火鸡的取值离鸭的中心很远，于是核函数饱和。线性边界只要求效应的**方向**在两个宿主间一致，不要求位置一致 —— 这恰恰是跨宿主迁移需要的性质。

这条修正了 §4 原先的读法。它不是多样性与核心保留度之间的交互作用 —— **核心保留度自己就承载了整个迁移。**

| 特征集 | 线性 AUC | 零最大 | p |
|---|---|---|---|
| 仅核心保留度 | 0.870 | 0.757 | **0.0020** |
| 全部 13 | 0.834 | 0.748 | **0.0020** |
| 仅 α 多样性 | 0.714 | 0.779 | 0.0140 |

### 5.2 效应是一个有方向的单一量，两个宿主一致

逐特征的迁移 AUC，以及每个特征在各宿主内部的效应方向：

| 特征 | 鸭 AUC | 火鸡 AUC | 方向一致？ |
|---|---|---|---|
| **CoreRetentionProportion** | **0.397** | **0.154** | **是（两边都 Pos↓）** |
| **CoreTaxaLost** | **0.603** | **0.846** | **是（两边都 Pos↑）** |
| CoreTaxaPresent | 0.397 | 0.154 | 是 |
| CoreMembershipProportion | 0.417 | 0.326 | 是 |
| Observed_Genera | 0.491 | 0.293 | 是 |
| Simpson / InvSimpson | 0.507 | 0.214 | 否 |
| CoreAbundanceRetention | 0.498 | 0.505 | 否 |

13 个特征中有 9 个在两个宿主里指向同一方向。最强的两个其实是同一件事的正反两面：**`CoreRetentionProportion` 在感染个体中更低，`CoreTaxaLost` 在感染个体中更高，鸭与火鸡皆然。**

> **把机制说白：感染个体丢失了更大比例的宿主特异基线核心菌群。构成这个核心的具体菌属在两个宿主间完全不同 —— 鸭 14 个属，火鸡 32 个 —— 但「丢失的比例」在两边表现一致。这正是分类学模型迁移不了、而这个模型能迁移的原因。**

`CoreRetentionProportion` 单独迁移到 0.846。它是在看过逐特征排名之后才挑出来的，因此必须用「13 个单特征取最大」的零分布来判：

```
最佳单特征              AUC 0.846（CoreRetentionProportion）
13 个取最大的零分布       0.623 ± 0.055（max 0.867）
选择偏倚校正后 p         0.0040
```

同样的校正用于 78 个特征对的搜索：实测最大 0.862（`Observed_Genera` + `CoreRetentionProportion`），搜索后的零分布 0.695 ± 0.051，**p=0.0033**。两次搜索都经受住了自身的选择偏倚。

### 5.3 这一节没有解决的问题

- α 多样性单独只到 0.714，而零分布最大值是 0.779 —— **它没有越过自身噪声上限**（p=0.0140 虽小于 0.05，但零分布最大值超过了实测值，说明单次噪声抽样就能压过它）。α 多样性不应被报告为可迁移。
- 预注册的量纲假设 —— 只有无量纲特征能迁移 —— **没有得到支持**。线性模型下计数型子集迁移到 0.856。量纲问题确实存在，但它存在于核函数里，不在特征里。

---

## 6. 全部合并会不会更好

最自然的下一步是把属丰度与生态特征拼在一起让模型自己挑。结论是：**不会更好，而且会同时毁掉生态特征那两个值得要的性质。**

### 6.1 队列内：没有增益

| 特征空间 | 特征数 | 鸭 | 特征数 | 火鸡 |
|---|---|---|---|---|
| 属丰度 | 70 | **0.836** | 62 | **0.972** |
| 属丰度 + α + 核心 | 83 | 0.832 | 75 | 0.960 |
| 属丰度 + α + 核心 + Bray | 93 | 0.831 | 85 | 0.979 |
| *纯生态，作参照* | *13* | *0.613* | *13* | *0.807* |

所有差异都小于重复间标准差（0.012–0.025）。鸭队列里合并反而略**差**于属丰度单独；火鸡队列里 `genus_eco` 更差、`genus_eco_bray` 略好。**这些都不是真实效应。**

原因在于生态特征并不是独立信息 —— 它们是**从同一张丰度表算出来的汇总统计量**。α 多样性是丰度向量的函数，核心保留度是对它设阈值后的计数。一个已经拿到全部 70 维丰度的模型，原则上可以自己重建这些量。没有新东西可加。

### 6.2 跨宿主：迁移被彻底破坏

| 鸭 → 火鸡 | AUC | 零最大 | p |
|---|---|---|---|
| **纯生态（α + 核心）** | **0.800** | 0.740 | **0.0050** |
| 属丰度 + α + 核心 | **0.490** | 0.810 | 0.522 |
| 属丰度单独 | 0.500 | — | 退化 |

**把属丰度加回来，合并模型从 0.800 掉到随机水平。** 属丰度特征是宿主特异的，在目标队列上取值远超训练范围；它们在标准化后的特征空间里占绝对多数，把本来能承载迁移的那 13 列生态特征淹没了。

### 6.3 笼效应：原封不动地继承

| 特征空间 | 纯笼 | 感染 | 笼 / 感染 | 显著 |
|---|---|---|---|---|
| **纯生态（α + 核心）** | **0.563** | **0.784** | **0.72** | **0/3** |
| 属丰度 | 0.908 | 0.967 | 0.94 | 3/3 |
| 属丰度 + α + 核心 | 0.906 | 0.956 | **0.95** | **3/3** |
| 属丰度 + α + 核心 + Bray | 0.811 | 0.962 | 0.84 | 2/3 |

合并模型追踪笼号的能力与属丰度单独完全一样（比值 0.95，3/3 显著）。**用干净特征去稀释一个受笼效应污染的特征块，并不能给它去污。**

### 6.4 结论

对这个问题而言，合并是错误的做法。**属丰度是更好的队列内分类器**，当问题是「在这个队列里能多准地检出感染」时应该用它。**生态特征是更好的跨宿主、更少受混杂的预测子**，当问题是「这个规律在别的宿主里还成立吗」时应该用它。**两者回答的是不同的问题；合并得到的模型对第一个问题没有改善，对第二个问题则完全失效。**

---

## 7. 生态类模型里该用哪个

### 7.1 先确立判断标准，比较才有意义

| 特征空间 | 鸭 | p | 火鸡 | p | 笼/感染 | 笼显著 | 能否迁移 |
|---|---|---|---|---|---|---|---|
| α 多样性 | 0.538 | 0.333 ✗ | 0.748 | 0.070 ✗ | 0.67 | 0/3 | 0.714 —— 未过零上限 |
| **核心保留度** | 0.593 | **0.050** | 0.799 | **0.010** | 0.77 | 0/3 | **0.870，p=0.0020** |
| α + 核心 | 0.613 | **0.010** | 0.807 | **0.020** | 0.72 | 0/3 | 0.834，p=0.0020 |
| Bray PCoA | 0.661 | **0.005** | 0.813 | **0.005** | 0.72 | 0/3 | 结构上不可能 |
| Bray 核 | **0.695** | **0.005** | 0.850 | **0.005** | — | — | 结构上不可能 |
| α + 核心 + Bray | 0.674 | **0.005** | **0.854** | **0.005** | 0.68 | 0/3 | 结构上不可能 |

单看队列内 AUC，Bray 系最强。**但这个比较不值得做**——属丰度是 0.836 与 0.972，队列内胜过全部生态空间（§1）。

> **如果问题只是「在这个队列里能多准地检出感染」，就不该用生态特征，直接用属丰度。** 会选生态空间的唯一理由，是要拿到属丰度**没有**的那两个性质：能跨宿主、不追踪隔离器。评判这些模型就应当按这两条来。

### 7.2 按这两条评，核心保留度胜出

**它是唯一能迁移的。** 所有 Bray 系空间**结构上**就跨不了队列 —— PCoA 的坐标轴与核方法的核宽都是队列内定义的，两个队列间没有共同坐标系。这不是效果差，是做不到。在三个可迁移的空间里：

```
仅核心保留度      0.870   零最大 0.757   p = 0.0020   ← 最好
α + 核心         0.834   零最大 0.748   p = 0.0020
仅 α 多样性       0.714   零最大 0.779   p = 0.0140   ← 零上限反超实测，不成立
```

**加上 α 多样性反而更差**（0.870 → 0.834）。α 在两个队列内都过不了自己的置换零分布，迁移时同样过不了 —— 它贡献的是噪声。

**它测得出感染、测不出隔离器。** 笼/感染比 0.77，3 个纯笼对比 0/3 显著（p 分别为 0.22、0.29、0.42），而感染效应 0.780（p=0.0066）；属丰度则是 0.94、3/3 全显著。

> α 多样性的比值 0.67 看起来更漂亮，但**比值只有在分子是真信号时才可读** —— α 在两个队列里都没有感染信号，它的比值没有意义。

**机制可写进论文。** 方向在两个宿主里一致：`CoreRetentionProportion` 在感染组更低（鸭 0.397，火鸡 0.154），`CoreTaxaLost` 更高（鸭 0.603，火鸡 0.846）。单个特征 `CoreRetentionProportion` 就能迁移到 0.846（选择偏倚校正后 p=0.0040）—— 这是一个可以写进摘要的量。

### 7.3 三条使用规则

1. **配线性模型，不要用 RBF。** 同一份特征：RBF 迁移 0.469，线性 0.870（§5.1）。
2. **不要加 α 多样性。** 它在这份数据里没有通过任何一项检验。
3. **不要与属丰度合并。** 迁移从 0.800 掉到 0.490，笼效应比值回到 0.95（§6）。

### 7.4 一句话总结

| 问题 | 用什么 |
|---|---|
| 这个队列里能多准地检出感染？ | **属丰度**（鸭 0.836 / 火鸡 0.972） |
| 这个规律在别的宿主里还成立吗？ | **核心保留度 + 线性模型**（0.870，p=0.0020） |

---

## 8. 下一步

1. **在第三个宿主上复现「鸭 → 火鸡」。** 这是价值最高的后续工作。天鹅队列不能胜任，需要另一个野外队列。
2. **把核心保留度的丢失作为可迁移量来报告**，跨宿主工作使用线性模型。§5 已定下机制，剩下的是确认它在第三个宿主上依然成立。
3. **给属丰度模型一个非退化的迁移基线** —— 用两个队列特征集的交集（`summary_Turkey_45_sample.md` §7 的待办）。战胜一个崩塌的模型是弱结论；战胜一个正常工作的模型才是强结论。
4. **做抽平，或显式建模深度。** 鸭队列深度已达 AUC 0.650。`--deconfound-depth` 的残差化是敏感性检查而非修复 —— 跨宿主迁移时它用的是源宿主的系数。
5. **不要把 Aitchison 距离当作分类学的生态学替代品。** 它就是同一份数据。Bray-Curtis 才是诚实的群落结构对照。

---

## 9. 复现

```bash
python3 ecological_models.py                                    # 主分析
python3 ecological_transfer_anatomy.py --n-perm 300             # §5 迁移拆解
python3 ecological_models.py --deconfound-depth --tag _depthadj # 深度敏感性
python3 ecological_cage_check.py --n-perm 300                   # 笼效应 vs 感染效应
```

| 文件 | 内容 |
|---|---|
| `results/ecological_models.json` | 队列内、置换检验、跨宿主迁移 |
| `results/ecological_models_depthadj.json` | 同上，去深度混杂版本 |
| `results/ecological_cage_check.json` | 各特征空间的纯笼效应与感染效应 |
| `results/ecological_transfer_anatomy.json` | 单特征 / 特征对搜索、量纲假设、线性对照、方向一致性 |
| `results/ecological_transfer_linear.json` | 线性迁移的置换检验与深度敏感性 |
| `results/ecological_depth_diagnostic.json` | 逐特征的单独 AUC 与深度相关性，分队列 |
| `results/*.log` | 每次运行的完整控制台输出 |

---

## 10. 尚未完成

- [ ] 在第三个宿主队列上复现「鸭 → 火鸡」的生态迁移
- [ ] 用两个队列特征集的交集给分类学模型一个非退化的迁移基线
- [ ] 做抽平或显式建模深度，然后重跑 §1 与 §4
- [ ] 把 `results/ecological_*.json` 纳入 `check_consistency.py`
- [x] ~~比较 α 多样性、核心保留度、群落结构与合并生态模型与属丰度模型~~ → 已完成（§1–§4）
- [x] ~~建立不含任何分类学信息的纯生态分类模型~~ → 已完成：纯生态空间（α + 核心，13 特征）即 §4 的正面结果
- [x] ~~搞清楚 α + 核心里到底是什么在迁移~~ → 已完成（§5）：核心保留度的丢失，由线性模型承载
